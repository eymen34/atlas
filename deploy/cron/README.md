# Cron manifests — placeholder

Example cron manifests (Cloud Scheduler, Kubernetes CronJob, GitHub Actions
cron) for the outbox-drain endpoint arrive in the deployment ticket. Until
then, this file exists only to track the directory in git.
